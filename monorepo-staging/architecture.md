Here is a spec for the project to unify all of my repositories. 
Step One each repository needs to be standardized in a long a bunch of different metrics. First, they must be standardized in their file structure file system structure including how they store vendor code, how they store style sheets and so on. Second, for each repository, they'll be given the same uplift for a variety of different code base improvements. 
The first codebase improvement is that each of them will use Import and Export module style. For those Imports and exports, each of the modules will have a clear intended scope so that it is obvious if a library contributes to it that another one can reuse it, append to it and so on. 
Each of the repositories have a common code base not only with respect to the vendors that they use, but also the common stock adapters for the Web browser with experience with respect to RDF and other serialization capabilities. 

The intent is that each repository that creates a function that is a duplicate or like reusable across other repositories has deduplicated those different functions so that they all follow the same functions and include only their respective in adapters as wrappers or Dom handlers or whatever. Will have their functions identified as potential duplicates, different maturity levels and so on. And that comparison will involve making a recommendation for some functions rather better than others to go into a common library that all of the apps will use. This common library can include smaller JavaScript files. Yes, but these common JavaScript files may be things having to do with input output. Things that have to do with serialization things that have to do with. Things that have to do with reading and writing specific formats. It may also have to do with certain adapters or helper functions such as identifying prefixes having commonly defined name spaces, the various ontologs and common relations.
I'm under the impression that each of the repositories will have a bit of a UI overhaul so that they begin to look more and more like each other, sharing a common workspace, navigation, and so on 
In order to plan this work, what I would like or would I rather need is a set of capabilities with user stories so that all of the work can be planned and effort levels can be assigned and business values can be assigned. In doing so, each of the user stories should have something like a unit test in view. Having these unit tests in view, we can take the spec and the user story and the unit test and we can use that same unit test across the different apps to make sure that the functions remain useful and stable across all the apps.
For each repository that has duplicate functions, I want to get the jest and see if the function can actually handle that unit test. Oose she consolidated and if there's genuine duplication even there then they could do deduplicated. Or organization of these different functions and JavaScript files? I'm also anticipating that the functions could be deduplicated in each repository before they're even considered across repositories, so it seems that that is a step to be taken early in the workflow. 
So I'm imagining a workflow. Something like this take a repository. Make sure all of its functions are deduplicated make sure they all have some jazzed tests. Then make sure that they're properly scoped into distinct modules and that the app works after all this is said and done. Then pairwise a repository can be given a comparison for each repository if there are ways to consolidate those JavaScript files into effectively libraries that they can be reused across the apps and an opportunity for promotion of certain functions from one application into the central library. Wherein the deduplication of functions just are put together. This is reiterative. Each repository gets this treatment of being standardized and then each repository gets this pairwise comparison for what sorts of things can be promoted up or not. The final shape of the library's might be a common thing that becomes a distribution file can be taken into a CDN or JS deliver. With a certain build number and brought in that way into each respective repository.
What is the final state of the repository or repositories? To actually be able to have each of these apps be shippable by themselves with a common set of libraries or shipped together, and I have in mind that it could be an installation wizard that sets up the ammo file for the get repo so that everything is put into the the Web appropriately, but I'm not sure.. outside of that, I think all of the websites should effectively get their own HTML page in a single repository where everything's put together. Everything's managed and I don't need to have multiple repositories anymore. Another option is that a single repository becomes the downstream derived product that pulls in the different repositories modules. So the main repository has a central Hub for all of the core JavaScript libraries and it also takes each of those other repositories and pulls them into a single app. But when it comes to those other repositories, they just take the common library and deploy their own small version of the modularized website. These are two different architectures and I'm willing to consider different ways of doing this as well.












