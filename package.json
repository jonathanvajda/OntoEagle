{
  "name": "ontoeagle-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-only",
  "description": "Static ontology search app with offline support",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "eslint . --quiet",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:coverage": "npm test -- --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "jest": "^29.7.0"
  }
}
